// Client to Server
export enum ServerCodes {
    INIT = "i",
    STATUS = "s",
    ROLL = "r",
    MESSAGE = "m",
}
// Server to Client
export enum ClientCodes {
    STATUS = "s",
    INIT = "i",
    TURN = "t",
    MESSAGE = "m",
}
