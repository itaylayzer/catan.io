export type MaterialList = [number, number, number, number, number];
export type DevcardList = MaterialList;

export enum Material {
    WOOD,
    WOOL,
    WHEAT,
    BRICK,
    ORE,
    GENERAL_DEAL,
}
